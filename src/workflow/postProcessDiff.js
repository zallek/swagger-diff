
/**
 * @param  {Array<Diff>} options.breaks       [description]
 * @param  {Array<Diff>} options.smooths      [description]
 * @param  {Array<RaxDiff>} options.unmatchDiffs [description]
 * @param  {String} versionDiff          [description]
 * @param  {Object} options.changes      [description]
 * @return
 * {
 *   errors: {Array>Diff>}
 *   warnings: {Array>Diff>}
 *   infos: {Array>Diff>}
 *   unmatchDiffs: {Array<RawDiff>}
 * }
 */
export default function({breaks, smooths, unmatchDiffs}, versionDiff, { changes }) {
}
