import prepareSwaggerSpec from './prepareSwaggerSpec';


export function swaggerDiff(previousSpec, nextSpec) {
  const previousPrepared = prepareSwaggerSpec(previousSpec);
  const previousPrepared = prepareSwaggerSpec(nextSpec);
}
